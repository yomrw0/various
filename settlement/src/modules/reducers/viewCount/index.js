import { VIEWCOUNT } from "../../actions/viewCount"

const init = {
    count : []
}

export default function viewCountReducer(state = init, action) {
    switch(action.type) {
        case VIEWCOUNT :
            return 
    }
}